<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
  <meta charset="UTF-8">
  <title>Financing — HorizonMotors</title>
  <link rel="stylesheet" href="/css/site.css" th:href="@{/css/site.css}">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    .grid{display:grid;gap:20px}
    .cols-3{grid-template-columns:repeat(3,minmax(0,1fr))}
    .cols-2{grid-template-columns:1.1fr 1fr}
    .card{border:1px solid #1c2632;border-radius:16px;background:#0b1118;padding:18px}
    .band{border-radius:16px;background:linear-gradient(90deg,#152131,#0f141b);padding:18px;border:1px solid #1c2632}
    @media (max-width:980px){.cols-2,.cols-3{grid-template-columns:1fr}}
  </style>
</head>
<body>
<header class="topbar">
  <div class="container inner">
    <a th:href="@{/}" class="brand">
      <img src="/img/logo.svg" width="28" height="28" alt="">
      <span>Horizon<span class="accent">Motors</span></span>
    </a>

    <nav class="nav-links">
      <a th:href="@{/}" th:classappend="${#httpServletRequest.requestURI == '/' ? ' active' : ''}">Home</a>
      <a th:href="@{/browse}" th:classappend="${#httpServletRequest.requestURI.startsWith('/browse') ? ' active' : ''}">Browse</a>
      <a th:href="@{/featured}" th:classappend="${#httpServletRequest.requestURI.startsWith('/featured') ? ' active' : ''}">Featured</a>
      <a th:href="@{/financing}" th:classappend="${#httpServletRequest.requestURI.startsWith('/financing') ? ' active' : ''}">Financing</a>
      <a th:href="@{/contact}" th:classappend="${#httpServletRequest.requestURI.startsWith('/contact') ? ' active' : ''}">Contact</a>
    </nav>
  </div>
</header>
<main class="section">
  <div class="container">
    <h1>Financing Options</h1>
    <p class="muted">Prime, sub-prime, new-to-credit — pick a plan that fits. No hidden fees.</p>

    <!-- Rate summary tiles -->
    <div class="grid cols-3" style="margin-top:14px">
      <div class="card"><div class="muted">Intro APR (OAC)</div><div style="font-size:28px;font-weight:700">0.9%–6.9%</div></div>
      <div class="card"><div class="muted">Lender Partners</div><div style="font-size:28px;font-weight:700">24+</div></div>
      <div class="card"><div class="muted">Pre-Approval Time</div><div style="font-size:28px;font-weight:700">≈ 5 min</div></div>
    </div>

    <!-- Two-column: explainer + calculator -->
    <div class="grid cols-2" style="margin-top:20px">
      <section class="card">
        <h3>How Financing Works</h3>
        <ol style="margin-left:18px">
          <li>Apply online (soft check only).</li>
          <li>See payment options from multiple lenders.</li>
          <li>Pick term, down payment, and protection plans.</li>
          <li>e-Sign and drive away the same day.</li>
        </ol>
        <div class="band" style="margin-top:14px">
          <strong>New to credit?</strong> We work with first-time buyers and newcomers. Ask about secured options.
        </div>
      </section>

      <section class="card">
        <h3>Quick Payment Estimator</h3>
        <form oninput="mth.value = ( (amount.value - down.value) * ( (rate.value/1200) / (1 - Math.pow(1 + rate.value/1200, -months.value)) ) ).toFixed(2)">
          <label>Vehicle Price</label>
          <input type="number" name="amount" value="32000" min="0" step="500" style="width:100%;margin:6px 0">
          <label>Down Payment</label>
          <input type="number" name="down" value="3000" min="0" step="500" style="width:100%;margin:6px 0">
          <label>APR %</label>
          <input type="number" name="rate" value="5.9" min="0" max="25" step="0.1" style="width:100%;margin:6px 0">
          <label>Term (months)</label>
          <input type="number" name="months" value="60" min="12" max="96" step="12" style="width:100%;margin:6px 0">
          <div class="band" style="margin-top:10px">Estimated Monthly: <span style="font-size:22px;font-weight:700">$<output name="mth">0.00</output></span></div>
          <div style="text-align:right;margin-top:10px"><a href="/contact" class="btn primary">Start Application</a></div>
        </form>
      </section>
    </div>

    <!-- FAQ -->
    <div class="card" style="margin-top:22px">
      <h3>FAQ</h3>
      <details><summary>Do you do pre-approval without affecting credit score?</summary><p>Yes — we use soft checks until you pick a final lender.</p></details>
      <details><summary>Can I finance with zero down?</summary><p>Often yes, OAC. Approval depends on income/credit profile.</p></details>
      <details><summary>Early payout penalties?</summary><p>Most lenders allow early payout; terms vary — we’ll show you up front.</p></details>
    </div>
  </div>
</main>

<footer>
  <div class="container cols">
    <div><div class="brand"><img src="/img/logo.svg" width="22" height="22" alt=""><span>Horizon<span class="accent">Motors</span></span></div>
      <p class="muted" style="max-width:520px">Performance & premium used vehicles. Serving GTA and beyond.</p></div>
    <div><div class="muted">Contact</div>
      <div><a href="tel:+14165551234">(416) 555-1234</a><br><a href="mailto:sales@horizonmotors.ca">sales@horizonmotors.ca</a></div></div>
  </div>
</footer>
</body>
</html>
