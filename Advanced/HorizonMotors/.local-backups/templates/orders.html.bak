<!doctype html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
  <head>
<link rel="stylesheet" th:href="@{/css/site.css}" href="/css/site.css">
<link rel="stylesheet" th:href="@{/css/site.css}" href="/css/site.css">
    <meta charset="UTF-8" />
    <title>Orders</title>
    <link rel="stylesheet" href="/css/site.css" />
  </head>
  <body>
<div th:replace="~{fragments :: header}"></div>
<main class="page container">
    <header class="topbar">
  <div class="container inner">
    <a th:href="@{/}" class="brand">
      <img src="/img/logo.svg" width="28" height="28" alt="">
      <span>Horizon<span class="accent">Motors</span></span>
    </a>

    <nav class="nav-links">
      <a th:href="@{/}" th:classappend="${#httpServletRequest.requestURI == '/' ? ' active' : ''}">Home</a>
      <a th:href="@{/browse}" th:classappend="${#httpServletRequest.requestURI.startsWith('/browse') ? ' active' : ''}">Browse</a>
      <a th:href="@{/featured}" th:classappend="${#httpServletRequest.requestURI.startsWith('/featured') ? ' active' : ''}">Featured</a>
      <a th:href="@{/financing}" th:classappend="${#httpServletRequest.requestURI.startsWith('/financing') ? ' active' : ''}">Financing</a>
      <a th:href="@{/contact}" th:classappend="${#httpServletRequest.requestURI.startsWith('/contact') ? ' active' : ''}">Contact</a>
    </nav>
  </div>
</header>
<div class="wrap card">
      <div class="content">
        <table>
          <thead>
            <tr>
              <th>ID</th>
              <th>Name</th>
              <th>Card</th>
              <th>Vehicle</th>
            </tr>
          </thead>
          <tbody>
            <tr th:each="o : ${orders}">
              <td th:text="${o.id}">1</td>
              <td th:text="${o.name}">Jane</td>
              <td th:text="${o.maskedCard}">****</td>
              <td th:text="${nameBy.apply(o.vehicleId)}">Tesla Model S</td>
            </tr>
          </tbody>
        </table>
        <div class="toolbar" style="margin-top: 12px">
          <a class="btn" href="/purchase?vehicleId=1">➕ New Purchase</a>
        </div>
      </div>
    </div>
  </main>
<div th:replace="~{fragments :: footer}"></div>
</body>
<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
<link rel="stylesheet" th:href="@{/css/site.css}" href="/css/site.css">
<link rel="stylesheet" th:href="@{/css/site.css}" href="/css/site.css">
  <meta charset="UTF-8"/>
  <title>Orders</title>
  <link rel="stylesheet" href="/css/site.css"/>
</head>
<body>
<div th:replace="~{fragments :: header}"></div>
<main class="page container">
  <header class="wrap">
<link rel="stylesheet" th:href="@{/css/site.css}" href="/css/site.css">
    <h1>Orders</h1>
    <div class="toolbar">
      <a class="btn" href="/">🏠 Home</a>
      <a class="btn" href="/inventory">🚗 Inventory</a>
    </div>
  </header>

  <div class="wrap card">
    <div class="content">
      <table>
        <thead>
          <tr><th>ID</th><th>Name</th><th>Card</th><th>Vehicle</th></tr>
        </thead>
        <tbody>
          <tr th:each="o : ${orders}">
            <td th:text="${o.id}">1</td>
            <td th:text="${o.name}">Jane</td>
            <td th:text="${o.maskedCard}">****</td>
            <td th:text="${nameBy.apply(o.vehicleId)}">Tesla Model S</td>
          </tr>
        </tbody>
      </table>
      <div class="toolbar" style="margin-top:12px">
        <a class="btn" href="/purchase?vehicleId=1">➕ New Purchase</a>
      </div>
    </div>
  </div>
</main>
<div th:replace="~{fragments :: footer}"></div>
</body>
</html>
