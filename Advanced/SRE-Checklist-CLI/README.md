# SRE-Checklist-CLI
CLI for SRE checklists/runbooks.

Setup & Run:
- `python3 -m venv .venv`
- `source .venv/bin/activate`
- `pip install -U pip`
- `pip install -r requirements.txt`
- `pytest -q`
- `python src/cli.py`
